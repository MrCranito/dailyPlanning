<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dailyplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">

        <receiver android:name=".helpers.BroadcastRecever"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".child.child_mainView">
        </activity>
        <activity android:name=".event.event">
        </activity>
        <activity android:name=".calendar.custom_view">
        </activity>

        <activity android:name=".alarm.alarm_mainView"></activity>

        <activity android:name=".alarm.alarm">

        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>



</manifest>